#订单服务
#server:
#  port: 8763

#统一配置 无论是在开发环境 还是在 测试环境 还是在生产环境 都可以共用的配置
#eureka server 服务注册中心的地址
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:8761/eureka
spring:
  application:
    name: order
#   datasource:
#     username: root
#     password: 123456
#     driver-class-name: com.mysql.jdbc.Driver
#     url: jdbc:mysql://127.0.0.1:3306/SpringCloud_Sell?characterEncoding=utf-8&useSSL=false
#   jpa:
#     show-sql: true
